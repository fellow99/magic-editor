server:
  port: 9999
magic-api:
  web: /magic/web
  prefix: /magic/web
  resource:
    type: file
    location: /data
#    type: database  # 配置接口存储方式，这里选择存在数据库中
#    table-name: magic_api_file_v2  # 数据库中的表名
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/magic?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: 123456