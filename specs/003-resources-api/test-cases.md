# 003-resources-api 测试用例

> 模块编号：003-resources-api
> 关联规范：[spec.md](./spec.md)
> 对应源码：`src/components/resources/magic-api-list.vue`、`magic-resource.css`
> 用例编号格式：`TC-003-NNN`
> 优先级：P0 = 阻断主流程，P1 = 关键功能，P2 = 增强/边界

---

## 1. 测试范围

**纳入**：
- 资源树加载与渲染（分组+接口合并、缩进、默认展开）
- 关键字搜索过滤
- 接口/分组的 CRUD（新建、修改、删除）
- 同组复制、跨组复制、分组复制
- 拖拽移动接口与分组（含循环引用校验）
- 锁定/解锁接口
- 路径复制（绝对/相对）
- 分组导出 zip
- 排序与折叠/展开
- 右键菜单
- Alt+G 快捷键
- bus 事件订阅与发射
- JavaClass API 发现器注册

**排除**：
- 接口脚本编辑（001 模块）
- 分组选择对话框 MagicGroupChoose 内部行为（006 模块）
- 树通用组件 MagicTree 内部细节（016 模块）
- 编辑器 tab 关闭副作用的具体实现（001 模块）

---

## 2. 环境前置

| 项 | 期望 |
|---|---|
| 后端 | magic-api 可访问，`group/list?type=1`、`list` 等端点可用 |
| 全局 | `bus`、`contants.SERVER_URL`、`contants.DEFAULT_EXPAND` 已配置 |
| 数据 | 至少存在 1 个分组与若干接口，1 个嵌套分组用于拖拽测试 |
| 通用组件 | MagicTree、MagicDialog、MagicInput、`$magicContextmenu` 已注册 |
| 浏览器 | 支持 `document.execCommand('copy')` 与拖拽 API |

---

## 3. 功能测试用例

### US-001 浏览资源树

#### TC-003-001 初始化并行加载分组与接口
- **关联**：FR-003-001、NFR-003-001、US-001
- **优先级**：P0
- **步骤**：刷新页面 / 触发 `initData()`
- **预期**：
  - 同时发出 `GET group/list?type=1` 与 `GET list`
  - 两者完成后才隐藏 loading

#### TC-003-002 树形组装：分组在前接口在后
- **关联**：FR-003-002
- **优先级**：P0
- **前置**：分组 A 含子分组 B 和接口 X
- **步骤**：观察渲染顺序
- **预期**：A 下渲染顺序为 B（folder=true）→ X（folder 缺省）

#### TC-003-003 节点 level/tmpName/tmpPath 计算
- **关联**：FR-003-003、NFR-003-002
- **优先级**：P1
- **前置**：分组 A(path=`/a`) > 子分组 B(path=`/b`) > 接口 X(path=`/x`)
- **步骤**：检查 X 节点
- **预期**：`level=2`，`tmpName=A/B/X`，`tmpPath=/a/b`，缩进 `level*17px`

#### TC-003-004 数据加载期间显示 loading
- **关联**：FR-003-004
- **优先级**：P1
- **步骤**：故意延迟后端响应
- **预期**：加载中 loading 可见，完成后隐藏

#### TC-003-005 默认展开所有分组
- **关联**：FR-003-005
- **优先级**：P1
- **前置**：`contants.DEFAULT_EXPAND=true`
- **预期**：所有分组节点 `opened=true`

#### TC-003-006 空数据时显示"无数据"
- **关联**：FR-003-006
- **优先级**：P2
- **前置**：后端返回空数组
- **预期**：MagicTree 显示无数据占位

---

### US-002 搜索接口

#### TC-003-010 关键字过滤树节点（实时）
- **关联**：FR-003-010、FR-003-011、FR-003-014
- **优先级**：P0
- **前置**：树包含分组"用户管理"含接口"用户查询"(`/user/query`)
- **步骤**：搜索框输入 `user`
- **预期**：分组与接口均可见；不匹配节点 `_searchShow=false` 隐藏

#### TC-003-011 子节点匹配则父分组保持可见
- **关联**：FR-003-012
- **优先级**：P0
- **步骤**：搜索仅命中接口名
- **预期**：父分组节点保持显示

#### TC-003-012 搜索按完整路径匹配
- **关联**：FR-003-011
- **优先级**：P1
- **步骤**：搜索 `用户管理/用户查询`
- **预期**：命中该接口

#### TC-003-013 大小写不敏感
- **关联**：FR-003-014
- **优先级**：P1
- **步骤**：搜索 `USER`
- **预期**：与 `user` 命中相同

#### TC-003-014 清空搜索框恢复全部
- **关联**：FR-003-013
- **优先级**：P1
- **步骤**：清空输入
- **预期**：所有节点 `_searchShow=true`

---

### US-003 点击接口在编辑器中打开

#### TC-003-020 点击接口发射 open 事件
- **关联**：FR-003-020、FR-003-021、US-003
- **优先级**：P0
- **步骤**：单击接口节点
- **预期**：bus emit `open`(接口对象)、`status`(状态消息)

#### TC-003-021 选中节点应用高亮 class
- **关联**：FR-003-022
- **优先级**：P1
- **预期**：被选节点拥有 `ma-tree-select` class

#### TC-003-022 双击分组节点切换展开/折叠
- **关联**：FR-003-104
- **优先级**：P1
- **步骤**：双击折叠态分组
- **预期**：`opened` 翻转

---

### US-004 右键菜单

#### TC-003-030 分组右键菜单项齐全
- **关联**：FR-003-110、FR-003-112
- **优先级**：P0
- **步骤**：分组节点右键
- **预期**：菜单含【新建接口、刷新、新建分组、修改分组、复制分组、删除分组、移动到根节点、导出】

#### TC-003-031 接口右键菜单项齐全
- **关联**：FR-003-111
- **优先级**：P0
- **步骤**：接口节点右键
- **预期**：菜单含【复制接口、复制接口到...、复制路径、复制相对路径、锁定/解锁、刷新接口、删除接口】

#### TC-003-032 菜单关闭后清除 selectRightItem
- **关联**：FR-003-113
- **优先级**：P2
- **步骤**：右键打开后关闭
- **预期**：节点 `selectRightItem=false`

---

### US-005 拖拽接口到分组

#### TC-003-040 拖拽接口到不同分组
- **关联**：FR-003-060、FR-003-066、US-005
- **优先级**：P0
- **前置**：接口 A 在分组 X
- **步骤**：拖拽 A 到分组 Y
- **预期**：发送 `POST api/move` `{id, groupId=Y.id}`，树更新，自动滚动到 A

#### TC-003-041 拖拽接口到同分组无操作
- **关联**：FR-003-063
- **优先级**：P1
- **步骤**：拖拽 A 仍放回 X
- **预期**：不发请求，树不变

#### TC-003-042 拖拽到接口节点（非分组）无效
- **关联**：FR-003-064
- **优先级**：P1
- **预期**：拖拽被拒绝

#### TC-003-043 拖拽视觉反馈
- **关联**：FR-003-065
- **优先级**：P2
- **预期**：目标分组节点显示红色边框

---

### US-006 拖拽分组

#### TC-003-050 拖拽分组到另一分组
- **关联**：FR-003-061、US-006
- **优先级**：P0
- **步骤**：拖拽分组 G1 到 G2
- **预期**：发送 `POST group/update`（含新 parentId）

#### TC-003-051 防止拖入自身子分组（循环引用）
- **关联**：FR-003-062、场景 5
- **优先级**：P0
- **前置**：A 含子分组 B
- **步骤**：拖拽 A 到 B
- **预期**：弹出 `不能移到 B`，不发请求

---

### US-007 分组 CRUD

#### TC-003-060 新建分组弹窗显示
- **关联**：FR-003-040
- **优先级**：P0
- **步骤**：右键"新建分组"
- **预期**：弹窗包含名称、前缀、父分组下拉

#### TC-003-061 新建分组名称非空校验
- **关联**：FR-003-041
- **优先级**：P0
- **步骤**：留空提交
- **预期**：弹出提示，未发请求

#### TC-003-062 新建分组调用 group/create
- **关联**：FR-003-042
- **优先级**：P0
- **步骤**：填写有效数据并保存
- **预期**：`POST group/create`；返回新 ID 后刷新树并滚动定位

#### TC-003-063 修改分组调用 group/update
- **关联**：FR-003-043、FR-003-046
- **优先级**：P0
- **步骤**：右键"修改分组" → 修改名 → 保存
- **预期**：`POST group/update`；树更新；锚点滚动

#### TC-003-064 删除分组确认 + 递归关闭 tab
- **关联**：FR-003-044、FR-003-045
- **优先级**：P0
- **步骤**：右键"删除分组" → 确认
- **预期**：弹出确认；确认后 `POST group/delete`；该分组下所有打开 tab 被关闭通知

#### TC-003-065 删除分组取消不发请求
- **关联**：FR-003-045
- **优先级**：P1
- **步骤**：弹出确认 → 取消
- **预期**：无请求，树不变

---

### US-008/US-009 复制接口/分组

#### TC-003-070 同组复制接口追加"(复制)"
- **关联**：FR-003-050、US-008
- **优先级**：P0
- **前置**：接口 A 已保存
- **步骤**：右键"复制接口"
- **预期**：树新增 `A(复制)`

#### TC-003-071 未保存接口复制被拒
- **关联**：FR-003-051
- **优先级**：P1
- **前置**：A.id 为空
- **步骤**：复制
- **预期**：弹出 `请先保存在复制！`

#### TC-003-072 跨组复制接口走分组选择对话框
- **关联**：FR-003-052
- **优先级**：P0
- **步骤**：右键"复制接口到..." → 选择目标分组 → 确认
- **预期**：MagicGroupChoose 弹出；确认后接口出现在目标分组

#### TC-003-073 复制分组调用 group/copy
- **关联**：FR-003-053、FR-003-054、US-009
- **优先级**：P0
- **步骤**：右键"复制分组" → 选择目标 → 确认
- **预期**：`POST group/copy {src, target}`；树新增分组副本

---

### US-010 锁定/解锁

#### TC-003-080 锁定接口
- **关联**：FR-003-070、FR-003-072、FR-003-073、US-010
- **优先级**：P0
- **前置**：接口 A 未锁定
- **步骤**：右键"锁定接口"
- **预期**：`POST lock {id}`；A 显示 `ma-icon-lock` 图标；菜单项变 `解锁`；emit `report`

#### TC-003-081 解锁接口
- **关联**：FR-003-071
- **优先级**：P0
- **前置**：A 已锁定
- **步骤**：右键"解锁"
- **预期**：`POST unlock {id}`；锁图标移除

---

### US-011 路径复制

#### TC-003-090 复制绝对路径
- **关联**：FR-003-080、FR-003-082、FR-003-084、US-011
- **优先级**：P0
- **前置**：`SERVER_URL=http://host`，接口 path=`/q`，groupPath=`/api/user`
- **步骤**：右键"复制路径"
- **预期**：剪贴板内容为 `http://host/api/user/q`，连续斜杠被 `replaceURL` 规范化

#### TC-003-091 复制相对路径
- **关联**：FR-003-081
- **优先级**：P0
- **预期**：剪贴板为 `/api/user/q`（不含 SERVER_URL）

#### TC-003-092 execCommand 失败时弹框展示路径
- **关联**：FR-003-083
- **优先级**：P1
- **前置**：模拟剪贴板权限拒绝
- **预期**：弹出 alert 含目标路径

---

### US-012 导出

#### TC-003-100 导出分组为 zip
- **关联**：FR-003-090、FR-003-091、FR-003-092、US-012
- **优先级**：P0
- **步骤**：右键分组"导出"
- **预期**：`GET /download?groupId=<id>`（responseType=blob）；浏览器下载 `<分组名>.zip`

---

### US-013 快捷键

#### TC-003-110 Alt+G 打开新建分组弹窗
- **关联**：FR-003-120、US-013
- **优先级**：P1
- **前置**：焦点在 `.ma-container`
- **步骤**：按 Alt+G
- **预期**：新建分组弹窗显示

---

### US-014/US-015 排序与折叠

#### TC-003-120 切换排序：升序 → 降序 → 原始
- **关联**：FR-003-100、FR-003-101、FR-003-102、US-014
- **优先级**：P1
- **步骤**：连续点击排序按钮 3 次
- **预期**：依次为 升序（中文按拼音 localeCompare）、降序、null（恢复原始）；分组始终在接口前

#### TC-003-121 一键折叠所有分组
- **关联**：FR-003-103、US-015
- **优先级**：P1
- **预期**：所有 `opened=false`

#### TC-003-122 一键展开所有分组
- **关联**：FR-003-103、US-015
- **优先级**：P1
- **预期**：所有 `opened=true`

---

### Bus 事件订阅

#### TC-003-130 logout 事件清空数据
- **关联**：FR-003-130
- **优先级**：P0
- **步骤**：emit `logout`
- **预期**：树数据为空

#### TC-003-131 opened 同步当前打开项
- **关联**：FR-003-023
- **优先级**：P1
- **步骤**：emit `opened`(item)
- **预期**：高亮该 item

#### TC-003-132 delete-api 触发删除
- **关联**：FR-003-025、FR-003-032、FR-003-033、FR-003-034
- **优先级**：P0
- **步骤**：emit `delete-api`(item)
- **预期**：弹出确认 → `POST delete {id}` → 树移除 → 通知关闭 tab

#### TC-003-133 refresh-resource 重新加载
- **关联**：FR-003-026
- **优先级**：P1
- **步骤**：emit `refresh-resource`
- **预期**：重新触发 group/list + list

#### TC-003-134 update-group 重建分组树
- **关联**：FR-003-027
- **优先级**：P1
- **步骤**：emit `update-group`(groups)
- **预期**：分组结构刷新但接口数据保留

#### TC-003-135 position-api 定位并打开
- **关联**：FR-003-024、场景 9
- **优先级**：P0
- **步骤**：emit `position-api`(id) 或 URL `?openIds=<id>`
- **预期**：节点滚动可视并 emit `open`

#### TC-003-136 点击分组发射 api-group-selected
- **关联**：FR-003-028
- **优先级**：P1
- **步骤**：单击分组节点
- **预期**：emit `api-group-selected`(folder)

---

### JavaClass API 发现器

#### TC-003-140 挂载时注册 API 发现器
- **关联**：FR-003-140、FR-003-141
- **优先级**：P1
- **步骤**：mount
- **预期**：`JavaClass.setApiFinder(...)` 被调用；回调返回的列表中路径经 `replaceURL` 处理

---

### US-016 E2E 持久化与刷新

#### TC-003-150 API 分组创建后刷新持久化验证
- **关联**：FR-003-001、FR-101-RES-02、US-016
- **优先级**：P0
- **前置**：后端 file 模式，数据目录 `/tmp/magic-api-data` 初始为空或可清洁
- **步骤**：
  1. 在左侧"接口"栏目点击"新建分组"，填写名称 `test1`，点击"创建"
  2. 验证 `test1` 出现在接口树中
  3. 检查数据目录：`/tmp/magic-api-data/api/test1/group.json` 存在
  4. 刷新浏览器页面
  5. 验证 `test1` 分组仍出现在接口树中
  6. 再次检查数据目录：`/tmp/magic-api-data/api/test1/group.json` 仍然存在
- **预期**：刷新前后 `test1` 均存在；数据文件未丢失

#### TC-003-151 API 多分组创建后刷新数据完整性验证
- **关联**：FR-003-001、FR-101-RES-01、US-016
- **优先级**：P0
- **前置**：TC-003-150 已执行，接口树中有 `test1` 分组
- **步骤**：
  1. 已存在 `test1` 分组的前提下，再次点击"新建分组"，填写名称 `test2`，点击"创建"
  2. 验证 `test2` 出现在接口树中，同时 `test1` 仍可见
  3. 检查数据目录：`/tmp/magic-api-data/api/test1/group.json` 和 `/tmp/magic-api-data/api/test2/group.json` 均存在
  4. 刷新浏览器页面
  5. 验证接口树中 `test1` 和 `test2` 均正常显示
  6. 再次检查数据目录：两个 `group.json` 文件均存在
- **预期**：刷新后两个分组均存在；数据目录文件完整

#### TC-003-152 函数分组创建后 API 分组数据隔离验证
- **关联**：FR-003-001、FR-004-004、FR-101-RES-01、US-016
- **优先级**：P0
- **前置**：TC-003-151 已执行，接口树中有 `test1`、`test2` 分组；函数树为空
- **步骤**：
  1. 切换到左侧"函数"栏目
  2. 点击"新建分组"，填写名称 `test3`，点击"创建"
  3. 验证 `test3` 出现在函数树中
  4. 检查数据目录：`/tmp/magic-api-data/function/test3/group.json` 存在
  5. 刷新浏览器页面
  6. 验证"函数"栏目中 `test3` 正常显示
  7. **切换回"接口"栏目**，验证 `test1` 和 `test2` 仍然正常显示
  8. 检查数据目录：`api/test1/`、`api/test2/`、`function/test3/` 三个分组文件均存在
- **预期**：函数分组创建不影响 API 分组数据；刷新后三类数据均完整

#### TC-003-153 API 创建→SQL 运行→保存→刷新→再运行完整链路
- **关联**：FR-003-020、FR-007-041、FR-008-002、FR-101-RES-04、US-016
- **优先级**：P0
- **前置**：接口树中已有分组
- **步骤**：
  1. 在接口树中右键点击分组 → "新建接口"
  2. 在底部"接口信息"面板中填写：接口名称 `sql_test`、请求路径 `/sql_test`
  3. 在中间 Monaco 编辑器中输入 SQL 语句：`select 'a' as val1`
  4. 点击右上工具条"运行（Ctrl+Q）"
  5. 检查运行反馈（底部"执行结果"面板）
  6. 点击右上工具条"保存（Ctrl+S）"
  7. 检查网络：`POST /resource/file/api/save`，Content-Type: text/plain，body 为 ROT13 编码，且包含 SQL 内容
  8. 检查数据目录：`/tmp/magic-api-data/api/<分组>/sql_test.ms` 文件存在且内容含 `select 'a' as val1`
  9. 刷新浏览器页面
  10. 验证接口树中 `sql_test` 仍然存在
  11. 点击 `sql_test` 加载，验证编辑器内容为 `select 'a' as val1`
  12. 再次点击"运行（Ctrl+Q）"
  13. 验证运行反馈正常
- **预期**：完整链路无异常；刷新后数据完整保留；运行功能在刷新前后均可用

---

#### TC-003-154 保存后刷新脚本内容持久化验证
- **关联**：FR-003-020、FR-101-RES-04、FR-101-RES-05、US-016
- **优先级**：P0
- **前置**：接口树中已有分组；后端 magic-api 2.2.2 + file 模式
- **步骤**：
  1. 右键接口分组 → "新建接口" → 名称 `script_persist`、路径 `/script_persist`
  2. 在中间 Monaco 编辑器中输入脚本 SQL：`select 'a' as val1`
  3. 点击右上工具条"保存（Ctrl+S）"
  4. 验证 Network：`POST /resource/file/api/save` 200，body 含 ROT13(SQL)
  5. 刷新浏览器页面
  6. 验证"接口"树中 `script_persist` 仍然存在
  7. 点击树中 `script_persist` 加载详情
  8. **检查 Monaco 编辑器内容**：必须显示 `select 'a' as val1`
  9. 同时验证底部"接口信息"面板中接口名称/路径与保存前一致
- **预期**：刷新后接口在树中存在；点击加载后编辑器正确显示保存前的 SQL 语句

---

## 4. 边界与异常

| 编号 | 场景 | 预期 |
|---|---|---|
| TC-003-200 | `lock` 字段为字符串 `'1'` 而非布尔 | 仍正确显示锁图标（AS 假设 2） |
| TC-003-201 | 分组 `parentId='0'` 视为根分组 | 正确放置在根级 |
| TC-003-202 | 接口 `method` 字段为空 | MagicTextIcon 渲染兜底（C-003-001 待澄清） |
| TC-003-203 | 同 ID 分组重复 | `pushFileItemToGroup` 命中第一个（C-003-002 待澄清） |
| TC-003-204 | 快速连续 `position-api` 请求 | 可能竞态，后到响应覆盖（C-003-003 待澄清） |
| TC-003-205 | 网络失败时 group/create | 树未新增，错误提示 |
| TC-003-206 | 删除接口已被后端删除 | 后端 4xx；前端仍移除节点并关闭 tab（确认现状） |
| TC-003-207 | 拖拽过程被 ESC 中断 | 不发请求，无视觉残留 |
| TC-003-208 | 搜索关键字含正则元字符（`*`、`(`）| 视为字面量匹配，不抛异常 |
| TC-003-209 | 复制接口名已含"(复制)" | 追加变成 `名(复制)(复制)` |
| TC-003-210 | 导出失败（5xx） | 不下载，提示错误 |

---

## 5. 用例索引摘要

| 用户故事 | 用例区间 | 数量 |
|---|---|---|
| US-001 树渲染 | TC-003-001 ~ 006 | 6 |
| US-002 搜索 | TC-003-010 ~ 014 | 5 |
| US-003 点击打开 | TC-003-020 ~ 022 | 3 |
| US-004 右键菜单 | TC-003-030 ~ 032 | 3 |
| US-005 拖拽接口 | TC-003-040 ~ 043 | 4 |
| US-006 拖拽分组 | TC-003-050 ~ 051 | 2 |
| US-007 分组 CRUD | TC-003-060 ~ 065 | 6 |
| US-008/009 复制 | TC-003-070 ~ 073 | 4 |
| US-010 锁定 | TC-003-080 ~ 081 | 2 |
| US-011 路径复制 | TC-003-090 ~ 092 | 3 |
| US-012 导出 | TC-003-100 | 1 |
| US-013 快捷键 | TC-003-110 | 1 |
| US-014/015 排序折叠 | TC-003-120 ~ 122 | 3 |
| Bus 事件 | TC-003-130 ~ 136 | 7 |
| JavaClass | TC-003-140 | 1 |
| E2E 持久化与刷新 | TC-003-150 ~ 154 | 5 |
| 边界与异常 | TC-003-200 ~ 210 | 11 |
| **合计** | | **67** |

> 优先级分布：P0 ≈ 28，P1 ≈ 24，P2 ≈ 15
